import { AppFileInputDirective } from './app-file-input.directive';

describe('AppFileInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFileInputDirective(null);
    expect(directive).toBeTruthy();
  });
});
